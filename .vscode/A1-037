#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main(){
    int divide[7] = {1000,500,100,50,10,5,1},dividetouse[4] = {1000,100,10,1},divide50[3] = {500,50,5};
    char roman[7] = {'M','D','C','L','X','V','I'},romantouse[4] = {'M','C','X','I'},roman50[3] = {'D','L','V'};
    int input,i,remainder,remainderloop;
    if(scanf("%d",&input)!=1) return 0;
    for(i = 0 ; i < 4;i++){
        //
        remainder = input / dividetouse[i];
        input = input % (dividetouse[i]);
        if(remainder == 5){
            printf("%c",roman50[i-1]);
        }
        else if(remainder > 5){
            if(remainder>8){
                for (remainderloop = 0; remainderloop < 10-remainder; remainderloop++)
                {
                    printf("%c",romantouse[i]);
                }
                printf("%c",romantouse[i-1]);
           }
           else
            {
                printf("%c",roman50[i-1]);
                for (remainderloop = 0; remainderloop <  remainder-5; remainderloop++)
                {
                    printf("%c",romantouse[i]);
                }
                
            }
        }        
        else if(remainder < 5){
           if(remainder>3){
                for (remainderloop = 0; remainderloop < 5 - remainder; remainderloop++)
                {    
                    printf("%c",romantouse[i]);
                }
               printf("%c",roman50[i-1]);
           }
           else
           {
                for (remainderloop = 0; remainderloop <  remainder; remainderloop++)
               {
                    printf("%c",romantouse[i]);
               }
           }
        }
    }
}