
#include <stdio.h>
#include <ctype.h>

int main(){
    int num,total=0;
    for (int i = 0;i < 2;i++){
        scanf("%d",&num);
        total += num;
    }
    printf("%d\n",total);
    if(total >= 50){
        printf("pass");
    }
    else
    {
        printf("fail");
    }
}