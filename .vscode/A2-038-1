#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>
#include <stdbool.h>
int getmax(int f1,int f2){
    if(f1 >= f2){
        return f1;
    }
    else
    {
        return f2;
    }
}
int main(){
    int input;
    scanf("%d",&input);
    int chars = getchar(),i,total_fire = 0,total_water = 0,total_earth = 0,total=0;
    for(i = 0;i < input;i++){
        int f1,w1,e1,f2,w2,e2;
        scanf("%d %d %d %d %d %d",&f1,&w1,&e1,&f2,&w2,&e2);
        total_fire += getmax(f1,f2);
        total_water += getmax(w1,w2);
        total_earth +=  getmax(e1,e2);
    }
    total = total_fire + total_earth +total_water;
    printf("%d\n",total);
    printf("%d %d %d\n",total_fire,total_water,total_earth);
    if(total_fire > total_water + total_earth){
        printf("YES");
    }
    else
    {
        printf("NO");
    }
}