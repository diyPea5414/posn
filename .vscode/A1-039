#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main(){
    int input,i;
    scanf("%d",&input);
    long total;
    for(i = 1; i <= input; i++)
    {
        double convert = i;
        total *= convert;
    }
    printf("%ld",total);
}