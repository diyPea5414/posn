#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main(){
    long totalnum;
    int i,input;
    if(scanf("%d",&input)!=1) return 0;
    long num = (input * (input + 1) * (2* input + 1))/6;
    printf("%ld",num);
}