#include <stdio.h>
#include <string.h>

int main(){
    int person[3],person2[3],i,age;
    scanf("%d",&person[0]); // year
    scanf("%d",&person[1]); // month
    scanf("%d",&person[2]); // day
    scanf("%d",&person2[0]); // year
    scanf("%d",&person2[1]); // month
    scanf("%d",&person2[2]); // day
    if(person[0] < person2[0]){
        printf("1");
    }
    else if(person[0]> person2[0])
    {
        printf("2");
    }
    else
    {
        if(person[1] < person2[1]){
            printf("1");
        }
        else if(person[1] > person2[1])
        {
            printf("2");
        }
        else
        {
            if(person[2] < person2[2]){
                printf("1");
            }
            else if(person[2] > person2[2])
            {
                printf("2");
            }
            else
            {
                printf("0");
            }
        }
    }
}