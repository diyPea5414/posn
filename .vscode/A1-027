#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(){
    char input[10];
    int i;
    if(scanf("%s",input) == 1){
        for (i = strlen(input)-1; i >= 0 ; i--)
        {
            printf("%c",tolower(input[i]));
        }
    }
}