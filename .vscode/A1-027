#include <stdio.h>
#include <string.h>

int main(){
    char input[10];
    int i;
    //scanf("%s",input);
    fgets(input,10,stdin);
    for (i = strlen(input)-2; i >= 0 ; i--)
    {
        printf("%c",input[i]);
    }
}